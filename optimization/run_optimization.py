import pandas as pd

def find_best_w(simulation_results, w_options, capacity_per_wagon, lambda_param):
    


def evaluate_waggon_option(revenue_arr, w, capacity_per_wagon, wagon_cost, lambda_param):
    return ""